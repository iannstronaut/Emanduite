{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum core capability for the main workspace window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
